import React from 'react';
import { View } from 'react-native';
import styles from '../styles/styles';

const TopBar = () => {
  return (
    <View style={styles.topBar}>
      {/* You can add more elements here like icons, logos, or user info */}
    </View>
  );
};

export default TopBar;